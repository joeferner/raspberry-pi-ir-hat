pub struct Signal {}

impl Signal {
    pub fn new(
        tolerance: f32,
        minimumSignals: u32,
        numberOfMatchingSignalsByLength: u32,
    ) -> Signal {
        return Signal {};
    }
}
